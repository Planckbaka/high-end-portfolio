# 📝 Articles 使用说明

## 快速开始

1. 创建 `.md` 文件
2. 添加 frontmatter
3. 编写内容
4. 运行 `npm run generate:articles`

## Frontmatter 格式

```yaml
---
title: "文章标题"
date: "2024-12-23"
category: "Go Design Pattern"
excerpt: "可选摘要，不填则自动提取"
---
```

| 字段 | 必需 | 说明 |
|------|:----:|------|
| `title` | ✅ | 文章标题 |
| `date` | ✅ | 日期 (YYYY-MM-DD) |
| `category` | ✅ | 分类名称 |
| `excerpt` | ❌ | 摘要（默认取前 150 字） |

## 命名规范

- ✅ `my-first-article.md`
- ✅ `learning-go-patterns.md`
- ❌ `My Article.md`（避免空格和大写）

## 添加图片

```markdown
![描述](/images/articles/my-image.png)
```

图片放置于 `public/images/articles/`，详见 [.IMAGE-GUIDE.md](./.IMAGE-GUIDE.md)

## 修改阅读时间

编辑 `scripts/generate-articles-data.mjs` 中的 `getReadingTime` 函数：

```javascript
// 当前：英文（每分钟 100 词）
const wordsPerMinute = 100;
return `${minutes} min read`;

// 中文优化：每分钟 400 字
const charsPerMinute = 400;
return `${minutes} 分钟阅读`;
```

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run generate:articles` | 更新文章数据 |
| `npm run dev` | 启动开发服务器 |

## 注意事项

- 文章按日期降序排列
- 以 `.` 开头的文件会被忽略
- 缺少必需字段的文章会被跳过
